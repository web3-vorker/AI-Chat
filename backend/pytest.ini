[pytest]
# Конфигурация pytest
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
markers =
    asyncio: asyncio tests
    integration: integration tests
    unit: unit tests
